

public class Main
{
    public void pvpGame(){
        boolean gameComplete = false;
        HumanPlayer p1 = new HumanPlayer('x');
        HumanPlayer p2 = new HumanPlayer('o');
        while(!gameComplete){
            //TicTacToe.playerTurn(TicTacToe.board, TicTacToe.filled, p1.symb);
            //TicTacToe.playerTurn(TicTacToe.board, TicTacToe.filled, p2.symb);
   
        }
    }
}
